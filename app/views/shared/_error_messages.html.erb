<% if flash[:error]&.any? %>
<div id="error_explanation">
  <div class=" alert alert-danger">
    The form contains <%= pluralize(flash[:error].count, "error") %>
  </div>
  <ul>
    <% flash[:error].each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
