<% if @feed_items&.any? %>

<%# ページネーションバー %>
<%= will_paginate(@feed_items) %>

<ol class="microposts">
    <%= render @feed_items %>
</ol>

<%# ページネーションバー %>
<%= will_paginate(@feed_items) %>

<% end %>