<% provide(:title,@user.name) %>
<aside class="col-md-4">
  <section class="user_info">
    <h1>
      <%= gravatar_for @user, {size: 200}%>
      <%= @user.name %>
    </h1>
  </section>
</aside>
</div>
