<%# ログインユーザと異なるユーザの場合のみ、フォロー、フォロー解除ボタンを表示する %>
<% if(current_user != @user) %>
<div id="follow_form">
    <%# まだフォローしていないならフォローボタンを、すでにフォロー済みならフォロー解除ボタンを表示する %>
    <% if current_user.following?(@user) %>
    <%= render "follow" %>
    <% else %>
    <%= render "unfollow" %>
    <% end %>
</div>
<% end %>